import styled from 'styled-components';

export const Container = styled.header`
  nav {
    font-weight: bold;
    margin-top: 0.5rem;

    ul {
      display: flex;
      justify-content: center;
      gap: 2rem;
    }
  }
`;
